package com.emelmujiro.secreto.mission.repository;

import com.emelmujiro.secreto.mission.entity.RoomMissionHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomMissionHistoryRepository extends JpaRepository<RoomMissionHistory, Long> {
}
